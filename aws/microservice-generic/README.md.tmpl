## Generic microservice application on AWS EKS 

The blueprint deploys a microservice application to AWS EKS.
XL deploy does the provisioning and deployment, while XL release orchestrates everything.

To deploy this blueprint with XL Platform follow the below steps

1. Apply the generated yaml configurations using XL CLI

    ```
    xl apply -f xebialabs.yaml
    ```

2. Go to XL Release and look for the {{.AppName}}-release-pipeline and start a new release from it.