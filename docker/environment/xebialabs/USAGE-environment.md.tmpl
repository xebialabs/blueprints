{{if .Standalone}}
To apply the file generated by this blueprint, execute the following command:

```
$ xl apply -f xebialabs/environment.yaml
```

This blueprint creates a `docker.Engine` infrastructure CI named `docker` and an environment that refers to that infrastructure CI.

Next, instantiate and apply the companion `docker/application` blueprint to create an application that can be deployed to this environment.
{{else}}
This blueprint creates a `docker.Engine` infrastructure CI named `docker` and an environment that refers to that infrastructure CI.
{{end}}
